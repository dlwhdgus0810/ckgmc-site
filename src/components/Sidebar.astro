---
import { findSection } from '../data/nav';
interface Props { path: string }
const { path } = Astro.props;
const section = findSection(path);
---
{section && section.items && (
  <div class="mb-3">
    <nav class="nav sidemenu flex-column" aria-label={section.label}>
      <h3 class="sidemenu-title mb-0 text-center">{section.label}</h3>
      {section.items.map((item, i) => (
        <div class:list={['nav-item', { 'border-bottom': i < section.items!.length - 1 }]}>
          <a href={item.href} class:list={['nav-link', { active: item.href === path }]}>{item.label}</a>
        </div>
      ))}
    </nav>
  </div>
)}
